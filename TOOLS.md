# GitLab MCP — Tool Reference

> Auto-generated by `go generate ./...` — do not edit manually.

---

## Contents

- [Projects](#projects)
- [Issues](#issues)
- [Merge Requests](#merge-requests)
- [Pipelines](#pipelines)
- [Jobs](#jobs)
- [Repositories](#repositories)
- [Users](#users)

---

## Projects

### `list_projects`

List GitLab projects. By default only returns projects the authenticated user is a member of.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `membership` | | `boolean` | If true, only return projects the authenticated user is a member of. If false, return all visible projects. Default: true. |
| `page` | | `number` | Page number for paginated results (default: 1). |
| `search` | | `string` | Filter projects by name or path (fuzzy match). |
| `visibility` | | `string` | Filter by visibility: "public", "internal", or "private". |

### `get_project`

Get detailed information about a single project.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path (e.g. "group/project"). |

### `list_group_projects`

List projects within a GitLab group.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `group_id` | ✓ | `string` | Group ID (numeric) or full path. |
| `include_subgroups` | | `boolean` | Include projects from subgroups. |
| `page` | | `number` | Page number for paginated results (default: 1). |
| `search` | | `string` | Filter projects by name or path. |

## Issues

### `list_issues`

List issues in a project. Returns compact references (no description body).

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path (e.g. "group/project"). |
| `assignee_username` | | `string` | Filter by assignee username. |
| `labels` | | `string` | Comma-separated label names to filter by. |
| `page` | | `number` | Page number for paginated results (default: 1). |
| `search` | | `string` | Search issues by title or description. |
| `state` | | `string` | Filter by state: "opened", "closed", or "all". Default: "opened". |

### `get_issue`

Get full details of a single issue, including description.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `issue_iid` | ✓ | `number` | The internal ID of the issue within the project. |

### `create_issue`

Create a new issue in a project.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `title` | ✓ | `string` | Title for the new issue. |
| `description` | | `string` | Markdown description body. |
| `labels` | | `string` | Comma-separated label names to apply. |

### `update_issue`

Update an existing issue's title, description, state, or labels.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `issue_iid` | ✓ | `number` | The internal ID of the issue within the project. |
| `description` | | `string` | New description (leave empty to keep current). |
| `labels` | | `string` | Comma-separated label names (replaces existing labels). |
| `state_event` | | `string` | State transition: "close" or "reopen". |
| `title` | | `string` | New title (leave empty to keep current). |

### `list_issue_notes`

List all comments on an issue.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `issue_iid` | ✓ | `number` | The internal ID of the issue. |

### `create_issue_note`

Add a comment to an issue.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `issue_iid` | ✓ | `number` | The internal ID of the issue. |
| `body` | ✓ | `string` | Markdown body of the comment. |

### `search_issues`

Search issues across all projects the user has access to.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `search` | ✓ | `string` | Search query string. |
| `page` | | `number` | Page number for paginated results (default: 1). |
| `state` | | `string` | Filter by state: "opened", "closed", or "all". Default: "opened". |

## Merge Requests

### `list_merge_requests`

List merge requests in a project. Returns compact references.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path (e.g. "group/project"). |
| `page` | | `number` | Page number for paginated results (default: 1). |
| `search` | | `string` | Search MRs by title or description. |
| `source_branch` | | `string` | Filter by source branch name. |
| `state` | | `string` | Filter by state: "opened", "closed", "merged", or "all". Default: "opened". |
| `target_branch` | | `string` | Filter by target branch name. |

### `get_merge_request`

Get full details of a single merge request.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `merge_request_iid` | ✓ | `number` | The internal ID of the merge request within the project. |

### `create_merge_request`

Create a new merge request.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `title` | ✓ | `string` | Title for the merge request. |
| `source_branch` | ✓ | `string` | Branch containing the changes. |
| `target_branch` | ✓ | `string` | Branch to merge into. |
| `description` | | `string` | Markdown description body. |
| `remove_source_branch` | | `boolean` | Delete source branch after merge. |
| `squash` | | `boolean` | Squash commits when merging. |

### `update_merge_request`

Update an existing merge request.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `merge_request_iid` | ✓ | `number` | The internal ID of the merge request. |
| `description` | | `string` | New description (leave empty to keep current). |
| `labels` | | `string` | Comma-separated label names (replaces existing labels). |
| `state_event` | | `string` | State transition: "close" or "reopen". |
| `target_branch` | | `string` | New target branch. |
| `title` | | `string` | New title (leave empty to keep current). |

### `merge_merge_request`

Accept and merge a merge request.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `merge_request_iid` | ✓ | `number` | The internal ID of the merge request. |
| `merge_commit_message` | | `string` | Custom merge commit message. |
| `should_remove_source_branch` | | `boolean` | Delete source branch after merge. |
| `squash` | | `boolean` | Squash commits when merging. |

### `approve_merge_request`

Approve a merge request.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `merge_request_iid` | ✓ | `number` | The internal ID of the merge request. |

### `list_mr_notes`

List all comments on a merge request.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `merge_request_iid` | ✓ | `number` | The internal ID of the merge request. |

### `create_mr_note`

Add a comment to a merge request.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `merge_request_iid` | ✓ | `number` | The internal ID of the merge request. |
| `body` | ✓ | `string` | Markdown body of the comment. |

### `create_mr_discussion`

Create a new discussion thread on a merge request diff.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `merge_request_iid` | ✓ | `number` | The internal ID of the merge request. |
| `body` | ✓ | `string` | Markdown body of the discussion comment. |
| `base_sha` | ✓ | `string` | Base commit SHA of the merge request diff. |
| `head_sha` | ✓ | `string` | Head commit SHA of the merge request diff. |
| `start_sha` | ✓ | `string` | Start commit SHA of the merge request diff. |
| `new_line` | | `number` | Line number in the new version to comment on. |
| `new_path` | | `string` | File path in the new version (for new/modified files). |
| `old_line` | | `number` | Line number in the old version to comment on. |
| `old_path` | | `string` | File path in the old version (for renamed/deleted files). |

### `list_mr_diffs`

List all file diffs in a merge request.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `merge_request_iid` | ✓ | `number` | The internal ID of the merge request. |

### `list_mr_commits`

List all commits in a merge request.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `merge_request_iid` | ✓ | `number` | The internal ID of the merge request. |

### `list_mr_pipelines`

List all pipelines for a merge request.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `merge_request_iid` | ✓ | `number` | The internal ID of the merge request. |

### `search_merge_requests`

Search merge requests across all projects the user has access to.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `search` | ✓ | `string` | Search query string. |
| `page` | | `number` | Page number for paginated results (default: 1). |
| `state` | | `string` | Filter by state: "opened", "closed", "merged", or "all". Default: "opened". |

## Pipelines

### `list_pipelines`

List pipelines in a project.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path (e.g. "group/project"). |
| `page` | | `number` | Page number for paginated results (default: 1). |
| `ref` | | `string` | Filter by git ref (branch or tag name). |
| `status` | | `string` | Filter by status: "running", "pending", "success", "failed", "canceled", "skipped", "manual". |

### `get_pipeline`

Get detailed information about a single pipeline.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `pipeline_id` | ✓ | `number` | The pipeline ID. |

### `create_pipeline`

Trigger a new pipeline on a ref.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `ref` | ✓ | `string` | Branch or tag name to run the pipeline on. |
| `variables` | | `string` | JSON object of key-value pairs for pipeline variables (e.g. {"KEY":"value"}). |

### `cancel_pipeline`

Cancel a running pipeline.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `pipeline_id` | ✓ | `number` | The pipeline ID. |

### `retry_pipeline`

Retry all failed jobs in a pipeline.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `pipeline_id` | ✓ | `number` | The pipeline ID. |

### `wait_pipeline_change`

Poll a pipeline until its status changes from the current value, or timeout.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `pipeline_id` | ✓ | `number` | The pipeline ID. |
| `poll_interval_seconds` | | `number` | Seconds between polls (default: 5, minimum: 3). |
| `timeout_seconds` | | `number` | Maximum seconds to wait (default: 300). |

### `wait_pipeline_finish`

Poll a pipeline until it reaches a terminal state (success, failed, canceled, skipped, manual), or timeout.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `pipeline_id` | ✓ | `number` | The pipeline ID. |
| `timeout_seconds` | | `number` | Maximum seconds to wait (default: 1800). |

## Jobs

### `list_pipeline_jobs`

List all jobs in a pipeline.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path (e.g. "group/project"). |
| `pipeline_id` | ✓ | `number` | The pipeline ID. |
| `scope` | | `string` | Filter by job status: "created", "pending", "running", "failed", "success", "canceled", "skipped", "manual". |

### `get_job`

Get detailed information about a single job.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `job_id` | ✓ | `number` | The job ID. |

### `get_job_log`

Get the trace/log output of a job. Supports grep filtering and head/tail truncation.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `job_id` | ✓ | `number` | The job ID. |
| `grep` | | `string` | Regex pattern to filter log lines. |
| `head` | | `number` | Return only the first N lines (or matched lines if grep is used). |
| `tail` | | `number` | Return only the last N lines (or matched lines if grep is used). Ignored if head is also set. |

### `retry_job`

Retry a failed or canceled job.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `job_id` | ✓ | `number` | The job ID. |

### `cancel_job`

Cancel a running job.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `job_id` | ✓ | `number` | The job ID. |

## Repositories

### `list_branches`

List branches in a project repository.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path (e.g. "group/project"). |
| `search` | | `string` | Filter branches by name (fuzzy match). |

### `get_branch`

Get details of a single branch.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `branch` | ✓ | `string` | Branch name. |

### `create_branch`

Create a new branch from a ref.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `branch` | ✓ | `string` | Name for the new branch. |
| `ref` | ✓ | `string` | Branch name, tag, or commit SHA to create the branch from. |

### `delete_branch`

Delete a branch from the repository.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `branch` | ✓ | `string` | Branch name to delete. |

### `list_repository_tree`

List files and directories in a repository tree.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `path` | | `string` | Path inside the repository to list (default: root). |
| `recursive` | | `boolean` | List tree recursively including subdirectories. |
| `ref` | | `string` | Branch, tag, or commit to list the tree at. |

### `get_file_content`

Get the content of a file from the repository.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `file_path` | ✓ | `string` | Path to the file within the repository. |
| `ref` | | `string` | Branch, tag, or commit to read the file at (default: default branch). |

### `list_commits`

List commits in a project repository.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `page` | | `number` | Page number for paginated results (default: 1). |
| `path` | | `string` | Filter commits affecting this file path. |
| `ref_name` | | `string` | Branch or tag name to list commits for. |

### `get_commit`

Get details of a single commit.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `sha` | ✓ | `string` | Commit SHA. |

### `compare_refs`

Compare two branches, tags, or commits.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `project_id` | ✓ | `string` | Project ID (numeric) or full path. |
| `from` | ✓ | `string` | Source branch, tag, or commit SHA. |
| `to` | ✓ | `string` | Target branch, tag, or commit SHA. |

## Users

### `get_user`

Get information about a GitLab user by ID.

| Parameter | Required | Type | Description |
|-----------|----------|------|-------------|
| `user_id` | ✓ | `number` | The user's numeric ID. |

### `get_current_user`

Get information about the currently authenticated user.

